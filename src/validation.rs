// use crate::nile::Move;
// use crate::tile::TilePlacement;

// pub fn is_valid(board: &Board, tile_placement: &TilePlacement) -> Result<(), String> {
//     if !board
//         .get_cell(tile_placement.coordinates)
//         .is_empty()
//     {
//         return Err("Cell already contains a tile".to_owned());
//     }

//     Ok(())
// }
